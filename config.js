const dateConfig = {
  timeZone: "Asia/Kolkata", // Set the time zone to IST
  hour12: true, // Use 12-hour format
  weekday: "short", // Display short weekday name (e.g., Mon)
  year: "numeric", // Display full year
  month: "short", // Display short month name (e.g., Jan)
  day: "numeric", // Display day of the month
  hour: "numeric", // Display hour
  minute: "numeric", // Display minute
  second: "numeric", // Display second
};

module.exports = { dateConfig };
